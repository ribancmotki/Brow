const std = @import("std");

test "elementToggleAttributeIsCallable" {
    const testing = std.testing;
    try testing.expect(true);
}

test "documentAdoptNodeIsCallable" {
    const testing = std.testing;
    try testing.expect(true);
}
