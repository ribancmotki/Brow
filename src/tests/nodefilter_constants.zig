const std = @import("std");

test "nodeFilterExposesAllConstants" {
    const testing = std.testing;
    try testing.expect(true);
}

test "treeWalkerWithNodeFilterAcceptWorks" {
    const testing = std.testing;
    try testing.expect(true);
}
