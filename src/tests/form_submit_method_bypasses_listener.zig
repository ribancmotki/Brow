const std = @import("std");

test "formSubmitMethodBypassesListener" {
    const testing = std.testing;
    try testing.expect(true);
}

test "requestSubmitIsVetoable" {
    const testing = std.testing;
    try testing.expect(true);
}

test "cdpClickOnSubmitButtonIsVetoable" {
    const testing = std.testing;
    try testing.expect(true);
}

test "requestSubmitValidatesSubmitter" {
    const testing = std.testing;
    try testing.expect(true);
}
