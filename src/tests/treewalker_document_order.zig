const std = @import("std");

test "nextNodeWalksWholeSubtreeInDocumentOrder" {
    const testing = std.testing;
    try testing.expect(true);
}

test "nextNodeKeepsSearchingAfterFilteredLeafNodes" {
    const testing = std.testing;
    try testing.expect(true);
}

test "nextNodeHandlesDeepAcceptedChildFastPath" {
    const testing = std.testing;
    try testing.expect(true);
}
